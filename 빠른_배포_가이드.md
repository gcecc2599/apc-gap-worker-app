# 🚀 빠른 배포 가이드

## 현재 상태
✅ 빌드 성공  
✅ Netlify CLI 설치 완료  
✅ 반응형 디자인 적용 완료

## 📋 배포 단계

### 방법 1: 자동 배포 (권장)

다음 명령어를 순서대로 실행하세요:

```powershell
# 1. Netlify 로그인
cd C:\Users\user\Desktop\webapp
netlify login

# 2. 프로젝트 초기화
netlify init
# 질문 답변:
#   - "Create & configure a new site" 선택
#   - 사이트 이름: apc-gap-record (또는 원하는 이름)
#   - 빌드 명령: npm run build
#   - 배포 디렉토리: dist

# 3. 프로덕션 배포 (공개 URL 생성)
netlify deploy --prod
```

### 방법 2: 수동 배포

1. **Netlify 웹사이트 접속**
   - https://app.netlify.com 접속
   - 무료 계정 생성 (또는 로그인)

2. **새 사이트 생성**
   - "Add new site" → "Deploy manually" 클릭
   - `dist` 폴더를 드래그 앤 드롭

3. **사이트 설정**
   - Site settings → Build & deploy
   - Build command: `npm run build`
   - Publish directory: `dist`

## 🌐 공개 URL

배포 완료 후 다음과 같은 URL이 생성됩니다:
- `https://your-site-name.netlify.app`
- 이 URL은 **인터넷 어디서나 접속 가능**합니다!

## 📱 반응형 지원

프로젝트는 이미 반응형으로 설계되어 있습니다:
- ✅ 모바일 (< 640px)
- ✅ 태블릿 (640px - 1024px)  
- ✅ 데스크톱 (> 1024px)

## 🔄 업데이트 배포

코드를 수정한 후 다시 배포:
```powershell
npm run build
netlify deploy --prod
```

## 📞 문제 해결

- **빌드 실패**: `npm run build` 로컬에서 먼저 테스트
- **배포 실패**: Netlify 대시보드의 로그 확인
- **도메인 변경**: Netlify 대시보드 → Domain settings
